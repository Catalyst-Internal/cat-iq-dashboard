# Loaded when APP_ENV=testing (e.g. php artisan test) so Dotenv does not read a missing .env.
# Real values for local dev belong in .env (gitignored); CI uses phpunit.xml <env> entries.

APP_ENV=testing
